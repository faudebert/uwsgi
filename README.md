# uwsgi

## How-to

```sh
$ docker compose build
$ docker compose up

$ curl localhost:8000
# You should see some JSON output on uWSGI term

$ cat default.log
# You should see uWSGI logs in this file
```
